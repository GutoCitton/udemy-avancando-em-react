const ExecutoFunction = ({myFunction}) => {
    return (
        <div>
            <button onClick={myFunction}>Clique aqui para exectuar a função!</button>
        </div>
    );
};

export default ExecutoFunction;